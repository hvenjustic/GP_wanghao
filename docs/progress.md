# 项目进度记录

> 用于记录当前仓库已经完成的部分、尚未完成的部分，以及接下来一到两个阶段的推进重点。  
> 维护规则：每次发生代码更新时，同步更新本文件中的“当前阶段”“已完成部分”“进行中部分”“下一阶段计划”。

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 面向中小电商的低代码订单管理系统 |
| 最近一次更新 | 2026-04-19 |
| 当前阶段 | 项目范围已冻结，以“主体闭环完成”作为本次实现与验收目标；当前进入验收、演示与文档收口阶段 |
| 参考文档 | [需求文档](requirements.md) / [技术路线文档](tech-roadmap.md) / [开发任务清单](dev-task-list.md) / [项目目标与阶段评估](project-review.md) |

## 1. 阶段判断

- 主体闭环：已完成并具备验收条件。真实登录权限、订单读写、状态流转、审计日志、低代码基础配置和规则工作台都已经落地。
- 范围管理：已冻结，不再以 `P1` 深化或 `P2` 扩展能力作为当前必须项。
- 当前仓库已不是纯文档或演示项目，而是一个可运行的业务后台原型，默认数据源为 Prisma + PostgreSQL。
- 当前重点不再是继续堆叠功能，而是做验收收口、演示准备和论文口径统一。

## 2. 已完成部分

### 2.1 工程与基础设施

- `已完成`：初始化 Next.js App Router + TypeScript 单仓项目。
- `已完成`：完成 `pnpm`、TypeScript、ESLint、Next.js 基础配置。
- `已完成`：建立 `src/app`、`src/components`、`src/features`、`src/lib`、`src/server`、`prisma` 目录结构。
- `已完成`：补充 `.env.example`、`.env.local`、`.env` 运行配置，默认使用远程 PostgreSQL。
- `已完成`：完成 Prisma Client、`db:push`、`db:seed` 和基础健康检查接口 `/api/health`。
- `已完成`：补充 `postinstall -> prisma generate`，减少 Ubuntu / Linux 部署时 Prisma Client 缺失导致的启动错误。
- `已完成`：补充 `pm2` 生产进程配置、统一管理脚本和 Ubuntu 部署说明，支持使用 `pm2` 管理 Next.js 生产进程。
- `已完成`：`pm2-manage.sh` 已放到项目根目录，服务器可直接执行 `./pm2-manage.sh` 管理进程。
- `已完成`：修正 `pm2` 监听地址配置，改用 `HOST` 而不是 Ubuntu 系统环境变量 `HOSTNAME`，避免生产进程被错误主机名触发重启。

### 2.2 页面与交互骨架

- `已完成`：实现项目总览页 `/`、登录页 `/login`、无权限页 `/forbidden`。
- `已完成`：实现订单列表页 `/orders` 与详情页 `/orders/[id]`。
- `已完成`：实现用户权限页 `/users`。
- `已完成`：实现低代码配置页 `/meta`。
- `已完成`：实现规则工作台 `/rules`。
- `已完成`：实现审计日志页 `/audit-logs` 与规则日志页 `/rule-logs`。
- `已完成`：首页与导航已去除项目阶段、推进计划等开发进度文案，改为系统能力和模块入口说明。
- `已完成`：登录页、订单页、详情页、用户页、低代码页、规则页等前端页面已统一去除“第一版 / 后续扩展 / 项目推进”等开发过程文案，改为中性的业务说明。
- `已完成`：登录页默认预填账号已调整为管理员账号，便于按主体闭环验收路径直接进入完整功能视图。

### 2.3 登录与权限

- `已完成`：实现基于数据库用户、角色、权限的真实登录校验。
- `已完成`：实现登录、退出、中间件登录拦截和页面级权限守卫。
- `已完成`：实现菜单权限、页面权限和接口鉴权。
- `已完成`：实现用户状态启停、角色调整、密码重置基础版。
- `已完成`：初始化管理员、运营、审核、配置四类账号、角色与权限关系。

### 2.4 订单业务闭环

- `已完成`：订单列表、详情、状态流转默认走 Prisma 读写。
- `已完成`：支持审核通过、审核驳回、锁单、解锁、异常标记、解除异常、分仓、发货等核心动作。
- `已完成`：支持批量审核通过、批量锁单、批量解锁、批量发货基础版。
- `已完成`：支持订单列表按当前筛选条件导出 CSV。
- `已完成`：支持订单导入模板下载、CSV 上传入口和基础导入校验结果回显。
- `已完成`：支持批量动作逐条结果回显，可在订单列表页查看成功/失败明细。
- `已完成`：支持关键动作原因校验，收紧驳回、锁单、解锁、改仓等操作约束。
- `已完成`：补充首轮订单业务约束收口，异常单不可分仓/发货，未分仓订单不可直接发货。
- `已完成`：补充异常处理联调，列表页与详情页统一展示异常原因、最近处理记录和恢复建议。
- `已完成`：操作日志、审计日志、物流记录和规则命中结果可落库查询。

### 2.5 低代码配置平台

- `已完成`：实体、字段、页面配置 CRUD。
- `已完成`：页面级版本治理，支持预览、发布、克隆新版本、回滚。
- `已完成`：实体/字段级版本治理，支持快照历史、依赖分析和按快照回滚。
- `已完成`：元数据依赖检测、快照落库和审计留痕。
- `已完成`：订单详情页低代码运行时基础版，已能按已发布实体/字段/页面配置动态渲染扩展字段区块。
- `已完成`：低代码运行时只加载已发布版本，草稿字段和草稿页面不会直接影响订单详情页。
- `已完成`：订单列表页已接入发布版低代码运行时，主表可按 `order_extension_list` 的已发布配置加载扩展列。
- `已完成`：低代码页面生效链路基础版已打通，订单列表页与详情页都会优先加载已发布页面版本，自动忽略未发布字段。
- `已完成`：低代码配置差异对比，实体、字段和页面版本现在都可以直接查看当前配置与历史快照/历史版本之间的差异项。
- `已完成`：低代码跨对象依赖拓扑，当前已能查看实体下字段、页面版本、规则版本之间的引用关系，并把规则依赖纳入字段删除约束。
- `已完成`：低代码批量发布与批量回滚，`/meta` 已支持按对象集合批量治理，并返回逐项执行结果。

### 2.6 规则编排引擎

- `已完成`：规则定义管理、版本保存、克隆、发布、回滚和试运行。
- `已完成`：React Flow 设计器，支持节点、连线和图结构存储。
- `已完成`：节点配置语义、字段/操作符/动作模板和模板载入能力。
- `已完成`：条件表达式库，条件节点已支持 `comparison / group / not` 三类表达式，并接入保存校验、试运行求值和订单规则执行链路。
- `已完成`：多分支执行器，分支节点已支持 `branches + defaultTarget` 路由配置，并接入规则保存校验、试运行和订单动作链路。
- `已完成`：规则可解释性增强，规则执行日志和订单详情现在都能看到命中原因、解释摘要和节点逐跳说明；历史路径数据也会自动降级生成解释信息。
- `已完成`：规则治理增强，已补规则定义启用/停用、已停用规则的发布/回滚拦截、回滚目标约束、已发布规则删除保护和一致的审计留痕。
- `已完成`：订单规则执行器，已接入审核通过、发货前校验、解除锁单、解除异常等订单动作链路。
- `已完成`：规则样例扩充，当前已内置自动审核、区域分仓、加急发货前校验、人工重跑复核四类已发布样例规则，并同步补了试运行默认输入和规则日志样例。

### 2.7 数据层与文档

- `已完成`：建立 `prisma/schema.prisma`，覆盖用户、角色、订单、元数据、规则、日志等核心模型。
- `已完成`：补充用户、角色、权限、订单、仓库、低代码配置、规则定义、规则版本、日志等演示种子数据。
- `已完成`：完善 [需求文档](requirements.md)、[技术路线文档](tech-roadmap.md)、[README](../README.md)。
- `已完成`：新增 [项目流程图](project-flow.md)，用于说明项目架构、系统总览、订单主流程、低代码配置链路和规则执行链路。

### 2.8 已验证结果

- `已完成`：`pnpm install`
- `已完成`：`pnpm db:generate`
- `已完成`：远程 PostgreSQL `SELECT 1` 连通性验证
- `已完成`：`pnpm db:push`
- `已完成`：`pnpm db:seed`
- `已完成`：`pnpm lint`
- `已完成`：`pnpm typecheck`
- `已完成`：`pnpm build`

## 3. 当前工作重点

- `进行中`：整理主体闭环验收口径，统一需求文档、进度文档、任务清单和汇报表述。
- `进行中`：准备项目演示路径，确保“登录 -> 订单处理 -> 字段配置发布 -> 规则执行”这条主链路可稳定展示。
- `进行中`：对当前已实现能力做回归检查，保证主体闭环不因后续整理受到影响。

## 4. 不纳入本次范围的能力

- `不纳入本次`：订单导入正式入库、去重策略和导入结果回写。
- `不纳入本次`：低代码表单页运行时动态渲染。
- `不纳入本次`：个人密码维护、角色治理细化、用户管理后台增强。
- `不纳入本次`：仓配工作台和运营统计看板。
- `不纳入本次`：第三方平台订单同步、物流接口、Webhook 回调。
- `不纳入本次`：售后、退款、换货等逆向流程。
- `不纳入本次`：轻租户隔离和多租户 SaaS 化能力。

## 5. 当前收口计划

当前不再继续新增功能，建议按以下顺序收口：

1. 先做主体闭环验收清单核对。
   目标是确认登录权限、订单主流程、低代码字段发布和规则执行四条主链路稳定可演示。
2. 然后整理项目文档与论文口径。
   目标是保证需求、技术路线、进度、任务清单和论文中的目标范围一致。
3. 最后准备项目展示材料。
   目标是形成可直接用于答辩或汇报的流程说明、演示顺序和结果截图。

## 6. 下次更新时需要同步的内容

- `最近一次更新` 日期是否已同步。
- 哪些模块从“进行中”进入“已完成”。
- 哪些任务从“未开始”进入“进行中”。
- 本次新增了哪些页面、接口、服务、数据模型或种子数据。
- 本次是否新增了可验证结果，例如命令通过、页面可访问、接口可调用。
- [项目目标与阶段评估](project-review.md) 的判断是否仍与代码现状一致。
- 范围冻结口径是否仍然成立，是否误把深化项重新写回本次目标。
